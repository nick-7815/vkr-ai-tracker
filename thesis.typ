//==============================================================================
// Пояснительная записка к ВКР бакалавра
//
// Это единственный файл, который нужно настроить под себя.
// Дальше пишите текст в chapters/ — вёрстку менять не нужно.
//==============================================================================

#import "lib/vkr.typ": vkr, table-of-contents, sources, abstract, unnumbered, appendix

#show: vkr.with(
  // Идентификатор ПЗ в верхнем колонтитуле.
  // NN — порядковый номер темы в приказе, YY — две последние цифры года.
  // Точные значения возьмите на кафедре.
  vkr-id: "ВКР – 40 461 806 – 0.27–NN–YY.81",

  // Титульный лист и задание выдаются на кафедре в готовом виде.
  // Отсканируйте их в один PDF, положите в other/ и раскомментируйте:
  //
  //   title-pages: muchpdf(read("other/title-and-task.pdf", encoding: none)),
  //
  // Пока файла нет, шаблон подставит три страницы-заглушки,
  // чтобы сквозная нумерация оставалась правильной.
  title-page-count: 3,
)

// --- Аннотации: страницы 4 и 5 ---------------------------------------------
#include "other/abstract-ru.typ"
#include "other/abstract-en.typ"

// --- Содержание: первый лист всегда страница 6 -----------------------------
#table-of-contents()

// --- Список сокращений (не обязателен, удалите если не нужен) --------------
#include "other/acronyms.typ"

// --- Введение и основные разделы -------------------------------------------
// Методичка требует не менее двух разделов; рекомендуемый план — пять.
#include "chapters/01-introduction.typ"
#include "chapters/02-analysis.typ"
#include "chapters/03-design.typ"
#include "chapters/04-implementation.typ"
#include "chapters/05-testing.typ"
#include "chapters/06-conclusion.typ"

// --- Список использованных источников --------------------------------------
// Не менее 30 источников, из них не менее 20 % зарубежных.
// Ссылок в тексте должно быть достаточно, чтобы упоминался каждый источник.
#sources(read("bibliography/references.bib"))

// --- Приложения -------------------------------------------------------------
// Буквы назначаются автоматически: А, Б, В, Г, Д, Е, Ж, И, К…
// (Ё, З, Й, О, Ч, Ь, Ы, Ъ пропускаются по ГОСТ)
#include "appendices/a-listing.typ"
